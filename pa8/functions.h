#include <stdio.h>
#include <stdlib.h>
#include <math.h>


char *str_n_copy(char *destination, char *source);

int binary_search(int list_values[], int target);

void bubble_sort();

int is_palendrome(char str[], int index_L, int index_R);